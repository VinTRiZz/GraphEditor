CONFIGURE_LIBRARY(GraphObject)

COMPONENTS_LINK_COMPONENT(GraphObject Logger)
COMPONENTS_LINK_COMPONENT(GraphObject Common)
COMPONENTS_LINK_COMPONENT(GraphObject Qt-Custom)

ADD_EDITOR_TEST(GraphObject)
